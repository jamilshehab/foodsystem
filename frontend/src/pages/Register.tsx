import RegisterComponent from "../components/Authentication/RegisterComponent";

const Register = () => {
  return (
    <main>
      <RegisterComponent />
    </main>
  );
};

export default Register;
